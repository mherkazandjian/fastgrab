"""Vendored Wayland protocol bindings, generated from XML by pywayland-scanner.

Regenerated infrequently; the source XML files in this directory plus the
canonical ``wayland.xml`` from ``/usr/share/wayland/`` are passed through
``pywayland-scanner -o ... -i ...`` and the relative ``..wayland`` imports
are rewritten to point at ``pywayland.protocol.wayland``.
"""
